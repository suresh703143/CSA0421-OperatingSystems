#include <stdio.h>

int main() {
    int f, n, i, j, hit, pf = 0, ptr = 0;
    int frame[10], ref[30];

    scanf("%d", &f);
    for(i=0;i<f;i++) frame[i] = -1;

    scanf("%d", &n);
    for(i=0;i<n;i++) scanf("%d", &ref[i]);

    for(i=0;i<n;i++){
        hit = 0;
        for(j=0;j<f;j++)
            if(frame[j] == ref[i]) hit = 1;

        if(!hit){
            frame[ptr] = ref[i];
            ptr = (ptr + 1) % f;
            pf++;
        }

        for(j=0;j<f;j++) printf("%d ", frame[j]);
        printf("\n");
    }

    printf("Page Faults = %d", pf);
}
