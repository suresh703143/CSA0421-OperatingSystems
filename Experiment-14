#include <stdio.h>

int main() {
    int f, n, i, j, k, hit, pf = 0, pos, far;
    int frame[10], ref[30];

    scanf("%d", &f);
    for(i=0;i<f;i++) frame[i] = -1;

    scanf("%d", &n);
    for(i=0;i<n;i++) scanf("%d", &ref[i]);

    for(i=0;i<n;i++){
        hit = 0;

        for(j=0;j<f;j++)
            if(frame[j] == ref[i]) hit = 1;

        if(!hit){
            int found = 0;
            for(j=0;j<f;j++)
                if(frame[j] == -1){
                    frame[j] = ref[i];
                    found = 1;
                    pf++;
                    break;
                }

            if(!found){
                int next[10];
                for(j=0;j<f;j++){
                    next[j] = 999;
                    for(k=i+1;k<n;k++)
                        if(frame[j] == ref[k]){
                            next[j] = k;
                            break;
                        }
                }

                pos = 0;
                for(j=1;j<f;j++)
                    if(next[j] > next[pos]) pos = j;

                frame[pos] = ref[i];
                pf++;
            }
        }

        for(j=0;j<f;j++) printf("%d ", frame[j]);
        printf("\n");
    }

    printf("Page Faults = %d", pf);
}
